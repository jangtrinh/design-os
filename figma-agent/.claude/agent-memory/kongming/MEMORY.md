# Memory Index — kongming (figma-agent)

- [broker-daemon-test-seam](project_broker-daemon-test-seam.md) — daemon harness = `startBrokerCore` extraction seam (timer-free core, injected shutdown, tickWatchdog(now)); env-var test mode rejected
