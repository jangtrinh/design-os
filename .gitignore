node_modules/
dist/
coverage/
*.local
*.log
.DS_Store

# Secrets / local credentials — never tracked. npm auth lives in ~/.npmrc (outside
# the repo); these guard any local env file that may hold tokens.
.env
.env.*
.npmrc
*.npmrc

# recall workspace: the vector index is a rebuildable cache over the JSONL ledger,
# never a source of truth — `recall index` regenerates it from scratch.
*.vec.db
*.vec.db-journal
*.vec.db-wal

# figma-agent workspace: probe/ scratch output (recon-site.mjs, network-capture.mjs, etc.)
RECON/
CLONE_AUDIT.md

# figma-agent broker runtime scratch (change-log / sync-state when the broker runs from the repo)
figma-agent/design/
/design/

# Generated website-study workspaces and lossless capture scratch. Curated benchmark
# source, contracts, reports, and optimized previews remain commit-eligible.
/experiments/qualified-delivery-reference-*/
/experiments/case-04-*/
/experiments/world-class-benchmark/assets/boards/
/experiments/world-class-benchmark/evidence/repeatability-study/runs/*/screenshots/
/experiments/world-class-benchmark/evidence/repeatability-study/runs/d01-raw-r1/
/experiments/world-class-benchmark/evidence/repeatability-study/run-packets/*-raw-*.json
/experiments/world-class-benchmark/evidence/repeatability-study/run-packets/*-enhanced-*.json
/experiments/world-class-benchmark/assets/media/*.png
/experiments/world-class-benchmark/evidence/repeatability-study/runs/d01-orchestrated-r2/hero-mountain-home.png
/experiments/world-class-benchmark/evidence/blind-curation/*/*/*.png
/experiments/world-class-benchmark/evidence/renders/
/specs/017-living-agent-proof/benchmark/renders/
/specs/017-living-agent-proof/benchmark/blind/

# Synthetic walkthrough scratch — re-generated by examples/synthetic/run.sh
examples/synthetic/output/

# Local agent-CLI dev environment — not part of the distributable repo.
# Exception: the studio-level librarian agent (spec 002) is a committed, static
# file so any machine gets the same knowledge keeper (Art VII multi-machine).
# Negation must re-include each parent dir before the one file can be un-ignored.
!.claude/
.claude/*
!.claude/agents/
.claude/agents/*
!.claude/agents/librarian.md
plans/
docs/journals/
release-manifest.json
.repomixignore
.fog-context/
.project-agent.md
.brv/

# Local symlinks into design-os-hq (private) corpus/ — taste ledger + third-party
# reference captures live there, never in this public repo.
/taste
/references
