node_modules/
dist/
coverage/
*.local
*.log
.DS_Store

# Secrets / local credentials — never tracked. npm auth lives in ~/.npmrc (outside
# the repo); these guard any local env file that may hold tokens.
.env
.env.*
.npmrc
*.npmrc

# recall workspace: the vector index is a rebuildable cache over the JSONL ledger,
# never a source of truth — `recall index` regenerates it from scratch.
*.vec.db
*.vec.db-journal
*.vec.db-wal

# figma-agent broker runtime scratch (change-log / sync-state / edit feed / memory
# events) when the broker runs with this repo as cwd. figma-agent itself lives in its
# own repo now (design-os-figma-plugin), but a broker on this machine can still be
# pointed here — this stays live, gitignored scratch either way.
/design/

# Generated website-study workspaces and lossless capture scratch. Curated benchmark
# source, contracts, reports, and optimized previews remain commit-eligible.
/experiments/qualified-delivery-reference-*/
/experiments/case-04-*/
/showcase/world-class-benchmark/assets/boards/
/showcase/world-class-benchmark/evidence/repeatability-study/runs/*/screenshots/
/showcase/world-class-benchmark/evidence/repeatability-study/runs/d01-raw-r1/
/showcase/world-class-benchmark/evidence/repeatability-study/run-packets/*-raw-*.json
/showcase/world-class-benchmark/evidence/repeatability-study/run-packets/*-enhanced-*.json
/showcase/world-class-benchmark/assets/media/*.png
/showcase/world-class-benchmark/evidence/repeatability-study/runs/d01-orchestrated-r2/hero-mountain-home.png
/showcase/world-class-benchmark/evidence/blind-curation/*/*/*.png
/showcase/world-class-benchmark/evidence/renders/
/showcase/017-living-agent-proof/benchmark/renders/
/showcase/017-living-agent-proof/benchmark/blind/

# Synthetic walkthrough scratch — re-generated by examples/synthetic/run.sh
examples/synthetic/output/

# Local agent-CLI dev environment — not part of the distributable repo.
# Exception: the studio-level librarian agent (spec 002) is a committed, static
# file so any machine gets the same knowledge keeper (Art VII multi-machine).
# Negation must re-include each parent dir before the one file can be un-ignored.
!.claude/
.claude/*
!.claude/agents/
.claude/agents/*
!.claude/agents/librarian.md
plans/
docs/journals/
release-manifest.json
.repomixignore
.fog-context/
.project-agent.md
.brv/

# Local symlinks into design-os-hq (private) corpus/ — taste ledger + third-party
# reference captures live there, never in this public repo.
/taste
/references
