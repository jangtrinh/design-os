# A13 WORLD-CLASS PASS — plugin panel craft-polish (preview.html vs Reference.png vs linear.app/vercel.com tiles vs spec.md A13)

Read fresh (this pass): full regenerated `preview.html` (byte-identical panel stylesheet per its own header comment, verified against `panel.html` claims via grep, not trusted from memory), `spec.md` A13 contract, `knowledge/taste-rubric.md` (the 6+1-axis model — used as the scoring instrument this round, more rigorous than the informal framework used in rounds 1-2), `references/Reference.png` (re-viewed round 1), 4 tiles from `linear.app.png.tiles/` and `vercel.com.png.tiles/` (craft-language only, marketing-page medium mismatch graded explicitly per A13's own instruction).

## Part A — Correctness spot-check

| Rule | Result | Evidence |
|---|---|---|
| Zero text glyphs (incl. `…`) in markup/CSS `content:` | PASS | `grep -n '…\|✓\|✗\|⟳\|•' preview.html` → 3 hits, ALL inside `/* CSS comments */` (L323 explains icons replaced `✗`/`✓`; L351-352 say "scrolls vertically…"/"…and NEVER horizontally"). None are in markup text or a `content:` property — the rule scopes to rendered/generated content, not authoring comments. Rendered activity text itself ("Creating a frame", "running") stays glyph-clean as fixed last round |
| One font-family | PASS | `grep 'font-family:' ` in panel scope (before the real `GALLERY-ONLY CSS` divider, L809) → only 4 hits: 3 are the compiled-but-**unused** Swiss-Monolith DS provenance tokens (`--text-*-font-family`, all "Inter, sans-serif" anyway, never consumed via `var()` — confirmed 0 usages), 1 is the live `font-family: var(--font-family-body)` on `body` (L335). Effective rendered family: exactly one |
| Exactly 3 font sizes, zero raw px | PASS | `grep 'font-size:\s*var'` → 16 hits, every one resolves to `--fga-font-title/body/caption` (L336,383,458,498,504,534,540,558,591,599,609,690,701,713,730,769). The compiled block's dead `--font-size-xs..4xl`/`--text-*-font-size` tokens are declared but never referenced via `var()` anywhere in a selector — confirmed by a second grep, 0 matches |
| No left accent bar in any spelling (owner veto, now absolute) | PASS | `grep 'fga-accent'` → **0 matches**, token fully deleted (not just unused). `grep 'box-shadow:\s*inset\|border-left'` → **0 matches**. The "running" row is now distinguished by background (`--fga-row-active`) + its own spinning icon only — no bar anywhere |
| A11a — top-edge highlight survives via `border-top-color`, not inset shadow | PASS | `.onboarding{border-top-color:var(--fga-topedge)}` L525, `.detail-cell{border-top-color:var(--fga-topedge)}` L585 — present, matches the phase-03 design exactly, no inset shadow anywhere in the file |
| No `transition: all` / bare `linear` on UI transitions | PASS | `grep 'transition:.*\ball\b\|transition:.*linear'` → 0 matches. The one `linear` in the file is `animation: fga-spin 1s linear infinite` (L679) — a continuous rotation, the rubric's own named exception (spin is not a state transition) |
| 4px spatial rhythm | PASS (spot-checked) | Every raw px literal found in padding/gap/margin (`4px`, `12px`) is a multiple of 4; token declarations (`--space-1:8px` etc) are all on-grid |
| **Contrast pairs — recomputed independently, not trusted from code comments** | **FAIL (1 pair, newly introduced this pass)** | Spot-checked 5 pairs against WCAG formula: `--fga-text-muted` on `--fga-row-active` = **4.55:1** ✓ (matches code comment exactly); `--fga-text-dim` on `--fga-surface` = **4.74:1** ✓ (matches comment); `--fga-tone-success` on `--fga-body` (text) = **5.96:1** ✓ (matches comment exactly); `--fga-tone-warning` on `--fga-body` (text) = **7.56:1** ✓ (matches comment exactly) — high confidence the file's own contrast provenance is accurate. **BUT**: `--fga-text-dim` on `--fga-row-active` = **3.96:1** (the file's OWN comment at L274-275 states this and warns *"never use it there"*) — yet `.activity-row.is-stale:hover{background:var(--fga-row-active)}` (L669) does exactly that: on hover, a stale row keeps its `--fga-text-dim` label/meta color (L658-659) while its background switches to `--fga-row-active`, landing at 3.96:1 — **below the 4.5:1 AA floor**, and below A12's own acceptance bar ("every text token clears 4.5:1... on `--fga-body`/`--fga-surface`/`--fga-row-active`"). This is a real, reachable interactive state (any stale row's hover, i.e. hovering to read a completed/failed row's tooltip) — a mechanical WCAG failure this exact pass introduced by pairing two individually-correct rules |
| Focus rings | PASS | `.sync-btn:focus-visible, .inline-link:focus-visible{outline:2px solid var(--fga-focus);outline-offset:2px}` L739-742 — present, 2px offset as A11 requires |
| Reduced-motion guard covers every animated/transitioned selector | PASS | L800-805 covers `.status-dot.is-pulsing::after`, the running spinner, `.activity-row` (both animation and transition), `.inline-link`/`.sync-prompt`/`.status-dot`/`.status-sentence` transitions |

**Part A verdict: FAIL — 1 blocking item.** Everything else this pass is clean, including all of round 2's prior fixes holding. The new failure is narrow and surgical to fix: either don't reapply `--fga-row-active` background to `.is-stale:hover`, or brighten the stale hover text color, or keep `--fga-text-dim` text but skip the hover-background swap for stale rows specifically.

## Part B — Excellence, scored against `knowledge/taste-rubric.md`'s 6-axis model (0-10 each, cited)

This round has the official rubric loaded for the first time (rounds 1-2 used an informal 5-point framework) — scores below are not comparable to the prior "74/90" numbers; they're a stricter, doctrine-grounded instrument.

| Axis | Score | Target band | Evidence |
|---|---|---|---|
| **Layout** | 8/10 | Low (rubric: "strict grids... right for dense data/admin tools") | Sticky status header (L419-421), 3-block dotted-divider pacing, `auto-fit minmax(120px,1fr)` grid degrades 1-col→3-col cleanly with width, single focal point per state (the status sentence). Correctly does NOT chase 9-10 (asymmetric/editorial) — that band is wrong for this persona per the rubric's own doctrine ("punishes axes the persona deliberately keeps restrained") |
| **Typography** | 8/10 | Low ("tight scale ~1.15-1.2, one family, minimal weight contrast... dashboards/tools") | 3 role-driven sizes (14/13/11, not a single modular ratio but each size is load-bearing: title=the one status line, body=every value, caption=labels/timestamps); tracking tuned per size (`--fga-track-title:-0.01em` tight, `--fga-track-caption:0.015em` open — matches rubric's exact large/small tracking guidance L77 of the rubric); leading tuned per role (1.35/1.5/1.45, matches rubric's "body leading ~1.5-1.6" almost exactly). Rubric's "body ≥16px" floor is a page-scale rule that cannot transfer to a 240px-wide tool chrome (16px body would force single-word wrapping) — same non-transfer logic A13 applies to Linear/Vercel's display sizes, applied in the opposite direction here |
| **Spacing** | 9/10 | Low/dense ("cockpit mode... admin, data tools") | 4px base unit holds with zero off-grid exceptions found; inner padding (`--space-1`=8px on `.detail-cell`) < outer (`--space-2`=16px on `.context`) — nesting reads correctly; density matches the dense-tool target exactly. `tap-target-undersized` (sync-btn ~27px tall) technically undershoots the 44px touch minimum, but this is a desktop-only, mouse-driven Figma plugin panel, not a touch surface — the rubric scopes that rule to touch surfaces explicitly, so not a deduction |
| **Motion** | 9/10 | Mid, low end ("hover/focus ~150-250ms natural easing... the dependable default") | 140ms sits at the fast edge of Mid, appropriate for a tool panel. `cubic-bezier(0.2,0,0,1)` ease-out, never linear on a UI transition (the one `linear` usage is the spin animation — the rubric's own named exception for continuous rotation). Every animated property is `transform`/`opacity`/`color`/`background-color`/`border-color`/`opacity`, zero layout properties. Comprehensive reduced-motion guard |
| **Iconography** | 10/10 | Mid-High (one consistent set, systematic) | One inline SVG family (Phosphor-style, `currentColor`, uniform `viewBox="0 0 256 256"`), one 14px icon grid + one 8px dot grid, explicitly reasoned as "one icon size, one dot size, no third" (L397-400 comment). Optical centring is a *derived formula*, not eyeballed: `(1.5em - 14px)/2` (L403-407) — directly answers the rubric's own scoring question ("is the icon optically aligned with adjacent text"). No emoji, no mixed styles |
| **Depth/Surface** | 9/10 | Low/flat ("hairline borders or background-color shifts... crisp, modern, right for dense UI") | Zero shadows, alpha-hairline borders only (`rgba(255,255,255,.08/.06/.03)`), never on a `background` (explicitly reasoned in the L298-301 comment to dodge taste-lint's `mode-invisible-surface` trap by name) — a textbook match to the rubric's Low descriptor. 3 named roles (raised-card border / hover fill / top-edge highlight), not one-off values |

**Composite: 53/60 → 88/100.** Below the round's requested ≥90 bar, but per the rubric's own stated doctrine (taste-rubric.md L354-355: *"a floor of 8 [per-axis] punishes axes the persona deliberately keeps restrained"*) this composite is the *correct* ceiling for a deliberately Low-target, dense tool panel — pushing Layout/Typography toward 9-10 would mean over-designing this surface into something the persona explicitly isn't. Reporting the honest number rather than force-fitting it past 90.

**Top-3 highest-leverage next steps:**
1. Fix the hover-contrast regression (Part A) — the one real defect this pass introduced.
2. Typography: consider a genuine modular ratio behind the 3 sizes (currently role-driven, not ratio-driven) if a future pass wants to push this axis past 8 — low priority, current approach is defensible.
3. Nothing else scored below 8; the panel is at a genuine craft ceiling for its target persona.

## Part C — Duel

### vs primary reference (`Reference.png`, layout signature)

| Trait | Status | Evidence |
|---|---|---|
| Two-tier surfaces | EMBODIED (upgraded) | `--fga-body`/`--fga-surface` unchanged from round 1, now layered with alpha-hairline depth (`--fga-hairline`/`-soft`/`-topedge`) — a more sophisticated rendition of the same signature, not a regression |
| Dotted dividers | EMBODIED | unchanged, `border-top:1px dotted var(--fga-divider)` throughout |
| Label-over-value | EMBODIED (upgraded) | `.detail-cell` now carries the alpha-hairline + top-edge treatment on top of the unchanged `dt`/`dd` structure |
| Active-row left accent | **MISSING (by owner decree, not a craft miss)** | Reference.png's "Account" row visibly carries a colored left bar on the active nav item. This pass deletes `--fga-accent` entirely and the left-bar treatment is explicitly vetoed (spec.md L252, "no left accent bar in any spelling... owner veto"; L473 code comment: "the owner vetoed the inset left-bar treatment entirely, 2026-07-29, screenshot"). This is a documented, locked design decision overriding the reference on this one trait, not an execution gap — flagged per A13's "cite a capture" rule, graded honestly as a loss against the primary reference, with the caveat that it's intentional |
| Underlined inline link | EMBODIED | unchanged |
| Footer identity row | EMBODIED | unchanged, now `#fga-version` id-addressable |

5 of 6 sub-signatures embodied or upgraded; 1 is an intentional, owner-locked removal. A13's pass rule is "no clear loss on ≥2 signatures" — only 1 signature shows a loss, and it's by decree — **duel against the primary reference: PASS**.

### vs craft-language captures (`linear.app.png.tiles/`, `vercel.com.png.tiles/` — transferable axes only)

| Axis | Transfers? | Status | Evidence |
|---|---|---|---|
| Hairline borders | Yes | EMBODIED | `linear.app` tile_0001: feature cards ("Purpose-built"/"Powered by agents") carry barely-visible ~1px borders on near-black — same alpha-subtlety the panel now uses via `--fga-hairline` (0.06-0.08 alpha white) on `.detail-cell`/`.onboarding`/`.sync-prompt` |
| Dark-surface treatment | Yes | EMBODIED | `vercel.com` tile_0000/0001 and `linear.app` tile_0000: near-black base (~#0A0A0A) with a barely-differentiated raised card tier (Linear's "Thread in #feedback" widget, Vercel's chat panel) — directly mirrors the panel's `#171717`/`#262626` restrained two-tier read |
| Muted-gray type hierarchy | Yes | EMBODIED | Linear's body copy ("Purpose-built for planning...") and Vercel's "Features" label/list sit in muted gray on near-black/white headline contrast — the panel's `--fga-text` (white) / `--fga-text-muted` / `--fga-text-dim` three-tier gray hierarchy mirrors this pattern exactly |
| Spacing discipline | Yes (in spirit only) | PARTIAL | Both sites show consistent, generous gaps at page scale — not literally comparable pixel-for-pixel to an 8px inter-row gap in a 240px panel, but the panel's own 4px-grid discipline (Part B) is the correctly-scaled tool-chrome analog of the same rhythm-discipline principle |
| Display type / hero treatment / page-scale whitespace | **No** | **N/A, correctly not scored** | Linear's 48px+ "The product development system..." headline (tile_0000) and Vercel's massive "Agentic Infrastructure" hero (tile_0000) are marketing-page-scale properties. The panel correctly does not attempt these at 240-640px tool-chrome scale — per A13's explicit instruction, this absence is not a loss |

Craft-language duel: **no clear loss on any transferable axis** — PASS.

## VERDICT: FIX

One blocking item, surgical: `.activity-row.is-stale:hover` (L669) drops `--fga-text-dim` label/meta text to **3.96:1** against `--fga-row-active`, below the 4.5:1 AA floor A12 itself requires on all three panel backgrounds — a real, reachable interactive state, not a decorative edge case. Everything else this pass checks out clean: correctness gate otherwise full-pass (glyphs, left-bar veto, token discipline, motion guards, focus rings), excellence composite 88/100 on the newly-loaded official 6-axis rubric (a rigorous, doctrine-grounded number appropriate to this surface's deliberately restrained "Low" persona target, not a craft deficiency), and the duel shows no unintentional loss against either the primary reference or the transferable craft-language axes from Linear/Vercel.
