Build a complete responsive landing page for a nutrition planning product called "Nourish Plan".

Audience: busy professionals who want practical weekly meal planning and nutrition guidance.
Primary action: create a weekly plan. Secondary action: explore how it works.

Deliver one standalone `index.html` with embedded CSS and JavaScript. Use the supplied images in
`assets/` when useful. The page must include navigation, hero, product explanation, planning
workflow, nutrition guidance, social proof, CTA, and footer. Add responsive behavior and working
interactions. Do not inspect files outside this run directory. Do not use external network assets.

Apply these project lessons without repeating them as visible copy:

1. Translate the brief into a design read, explicit art direction, topic-specific section
   architecture, asset roles, interaction decisions, and qualification checks before coding.
2. Real imagery is design material. Use images for section storytelling instead of replacing
   visual opportunities with decorative diagrams, empty patterns, or generic text cards.
3. Invest in every major section. The visual and interaction quality below the hero must not
   decline; qualify each section independently.
4. Use a distinct layout family for each topic. Avoid repetitive equal-card rows and redundant
   split sections. Use bold whitespace within a coherent grid.
5. Use an icon library style consistently; never use text arrows or Unicode glyphs as interface
   icons. Since this is offline standalone HTML, use accessible CSS-drawn primitive marks only
   where no icon asset exists, and do not fake complex icons.
6. Include gentle hero background motion, motivated scroll reveals, loading/default-state
   transitions, custom selection controls, reduced-motion support, and explicit mobile collapse.
7. Keep the full page visually coherent: one palette, one radius logic, accessible contrast,
   realistic copy, and no quality collapse in the footer.

Complete the page autonomously and write only inside this run directory.
